# Modern Coffee

This app allows the user to create and edit coffee recipes. The app comes pre-loaded with dozens of brew methods and variables, and users can add their own.

The app requires an account. User management is handled through Firebase/auth and the user's custom recipes are stored in a Firebase realtime database.
